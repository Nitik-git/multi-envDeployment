name: api-prod-workflow
on:
  # workflow_dispatch:
  push:
    branches:
      - master                                                          // add button to manual trigger

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: hello world
        run: echo "Hello prod World"
        shell: bash
